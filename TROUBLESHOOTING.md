# FastSearch MCP Troubleshooting Guide
**Auto-generated**: 2025-08-27 09:55

## Issue Detected: Missing Python Module  
**Error**: `No module named fastsearch.server` (inferred from pattern)

### Quick Fix
```bash
# Install in development mode
pip install -e .

# Verify package structure
python -c "import fastsearch; print('✅ Module found')"
```

### Check Package Structure
Ensure these files exist:
- `setup.py` or `pyproject.toml` ✅ 
- `src/fastsearch/__init__.py`
- `src/fastsearch/server.py` or `src/fastsearch/main.py`
- Entry points configured correctly

### Missing Requirements.txt
This repo has no requirements.txt file. Create one:
```bash
echo "fastmcp>=2.10.1" > requirements.txt
pip install -r requirements.txt
```

### Windsurf Cascade Prompt
```
Fix fastsearch-mcp Python package installation and module structure. Ensure proper setup.py/pyproject.toml configuration, create requirements.txt with FastMCP dependency, install in development mode, and verify module imports work correctly.
```

### Test Command
```bash
python -m src.fastsearch.main  # or appropriate entry point
```

### Verification Steps  
1. Run `pip install -e .` in repo root
2. Create `requirements.txt` if missing
3. Test import: `python -c "import fastsearch"`
4. Check entry points in setup.py
5. Check Claude logs: `mcp-server-fastsearch.log`

### Log Location
`C:\Users\sandr\AppData\Roaming\Claude\logs\mcp-server-fastsearch.log`

---
*Auto-generated by MCP Troubleshooter - Remove this file when fixed*